spring:
  datasource:
    url: jdbc:mysql://$sql7.freesqldatabase.com:3306/$sql7816473?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: sql7816473
    password: P6GAXB439F

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: ${PORT:8080}
